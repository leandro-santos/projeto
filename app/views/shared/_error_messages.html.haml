.content_
  - if resource.errors.any?
    %div(id="error_explanation")
      %h2
        = t "error_message_header", :count => resource.errors.count, :model => resource.class.model_name.human
      %ul
        - resource.errors.full_messages.each do |msg|
          %li
            = msg